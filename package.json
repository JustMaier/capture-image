{
  "name": "capture-image",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node ./test/test.js",
    "update": "git pull",
    "build": "docker compose build image-capture",
    "docker:deploy": "docker compose down && docker compose up -d",
    "deploy": "npm run update && npm run build && npm run docker:deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^21.4.1"
  }
}